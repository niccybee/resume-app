# Pin publication to an explicit CV Revision

A CV's stable public slug points to one explicitly selected immutable CV Revision. Finishing an Editing Session never changes the Published Revision; publishing another Revision atomically moves the slug, and selecting an older Revision provides rollback. This adds an explicit publication step but prevents unfinished or newly completed work from silently changing a public CV.
